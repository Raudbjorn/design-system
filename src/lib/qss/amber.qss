/* GENERATED by scripts/build-tokens.mjs — do not edit by hand.
 * Theme: amber — Qt Style Sheet for @svnbjrn/design.
 *
 * Apply whole-app:  app.setStyleSheet(Path(".../qss/amber.qss").read_text())
 *
 * Variant selectors use dynamic properties, e.g.
 *   button.setProperty("class", "primary")
 * Qt does NOT restyle on property changes — re-polish after setProperty:
 *   button.style().unpolish(button); button.style().polish(button); button.update()
 */

* {
  font-family: "Inter", system-ui, sans-serif;
  font-size: 14px;
  color: #e2d9c8;
}

QMainWindow, QDialog {
  background: #0f0e0d;
}

QWidget#svSurface {
  background: #1a1815;
}

/* ── Buttons ─────────────────────────────────────────────────────────── */

QPushButton {
  background: #221f1a;
  color: #e2d9c8;
  border: 1px solid #332e26;
  border-radius: 6px;
  padding: 8px 16px;
  font-weight: 550;
}
QPushButton:hover {
  background: #3e3b37;
}
QPushButton:pressed {
  background: #52504b;
}
QPushButton:focus {
  border: 2px solid #d4a017;
  padding: 7px 15px;
}
QPushButton:disabled {
  background: #1a1815;
  color: #948a76;
  border-color: #332e26;
}

QPushButton[class="primary"] {
  background: #d4a017;
  color: #0f0e0d;
  border: none;
  padding: 9px 17px;
}
QPushButton[class="primary"]:hover {
  background: #dbaf4f;
}
QPushButton[class="primary"]:pressed {
  background: #dfb867;
}
QPushButton[class="primary"]:focus {
  border: 2px solid #f1e7c4;
  padding: 7px 15px;
}
QPushButton[class="primary"]:disabled {
  background: #2b271f;
  color: #948a76;
}

QPushButton[class="danger"] {
  background: #e4635a;
  color: #0f0e0d;
  border: none;
  padding: 9px 17px;
}
QPushButton[class="danger"]:hover {
  background: #eb7c72;
}
QPushButton[class="danger"]:pressed {
  background: #ef8c83;
}
QPushButton[class="danger"]:focus {
  border: 2px solid #f1e7c4;
  padding: 7px 15px;
}
QPushButton[class="danger"]:disabled {
  background: #2b271f;
  color: #948a76;
}

QPushButton[class="ghost"] {
  background: transparent;
  color: #a89c86;
  border: none;
  padding: 9px 17px;
}
QPushButton[class="ghost"]:hover {
  background: #221f1a;
  color: #e2d9c8;
}
QPushButton[class="ghost"]:pressed {
  background: #52504b;
}
QPushButton[class="ghost"]:disabled {
  color: #948a76;
}

/* ── Text inputs ─────────────────────────────────────────────────────── */

QLineEdit, QTextEdit, QPlainTextEdit, QSpinBox, QDoubleSpinBox, QComboBox {
  background: #221f1a;
  color: #e2d9c8;
  border: 1px solid #332e26;
  border-radius: 4px;
  padding: 8px 12px;
  selection-background-color: #d4a017;
  selection-color: #0f0e0d;
}
QLineEdit:focus, QTextEdit:focus, QPlainTextEdit:focus, QSpinBox:focus, QDoubleSpinBox:focus, QComboBox:focus {
  border: 2px solid #d4a017;
  padding: 7px 11px;
}
QLineEdit:disabled, QTextEdit:disabled, QPlainTextEdit:disabled, QSpinBox:disabled, QDoubleSpinBox:disabled, QComboBox:disabled {
  background: #1a1815;
  color: #948a76;
}
QLineEdit[hasError="true"] {
  border: 2px solid #e4635a;
  padding: 7px 11px;
}

QComboBox::drop-down {
  border: none;
  width: 24px;
}
QComboBox QAbstractItemView {
  background: #221f1a;
  border: 1px solid #332e26;
  border-radius: 6px;
  selection-background-color: #2b271f;
  selection-color: #d4a017;
  outline: 0;
}

/* ── Menus ───────────────────────────────────────────────────────────── */

QMenuBar {
  background: #1a1815;
  border-bottom: 1px solid #332e26;
}
QMenuBar::item {
  padding: 6px 12px;
  background: transparent;
}
QMenuBar::item:selected {
  background: #2b271f;
  border-radius: 4px;
}

QMenu {
  background: #221f1a;
  border: 1px solid #332e26;
  border-radius: 10px;
  padding: 4px;
}
QMenu::item {
  padding: 8px 12px;
  border-radius: 4px;
  min-height: 28px;
}
QMenu::item:selected {
  background: #2b271f;
}
QMenu::item:disabled {
  color: #948a76;
}
QMenu::separator {
  height: 1px;
  background: #332e26;
  margin: 4px 8px;
}

QToolTip {
  background: #2b271f;
  color: #f1e7c4;
  border: 1px solid #332e26;
  border-radius: 4px;
  padding: 4px 8px;
  font-size: 12px;
}

/* ── Scrollbars ──────────────────────────────────────────────────────── */

QScrollBar:vertical {
  background: transparent;
  width: 8px;
  margin: 0;
}
QScrollBar:horizontal {
  background: transparent;
  height: 8px;
  margin: 0;
}
QScrollBar::handle:vertical, QScrollBar::handle:horizontal {
  background: #332e26;
  border-radius: 4px;
  min-height: 24px;
  min-width: 24px;
}
QScrollBar::handle:vertical:hover, QScrollBar::handle:horizontal:hover {
  background: #948a76;
}
QScrollBar::add-line, QScrollBar::sub-line {
  height: 0;
  width: 0;
}
QScrollBar::add-page, QScrollBar::sub-page {
  background: transparent;
}

/* ── Tabs ────────────────────────────────────────────────────────────── */

QTabWidget::pane {
  border: 1px solid #332e26;
  background: #1a1815;
  border-radius: 6px;
}
QTabBar::tab {
  background: transparent;
  color: #a89c86;
  padding: 8px 12px;
  border-bottom: 2px solid transparent;
}
QTabBar::tab:selected {
  color: #d4a017;
  border-bottom: 2px solid #d4a017;
}
QTabBar::tab:hover:!selected {
  color: #e2d9c8;
}

/* ── Checkables ──────────────────────────────────────────────────────── */

QCheckBox, QRadioButton {
  spacing: 8px;
  color: #e2d9c8;
}
QCheckBox::indicator, QRadioButton::indicator {
  width: 16px;
  height: 16px;
  border: 1px solid #332e26;
  background: #221f1a;
}
QCheckBox::indicator {
  border-radius: 4px;
}
QRadioButton::indicator {
  border-radius: 8px;
}
QCheckBox::indicator:hover, QRadioButton::indicator:hover {
  border-color: #d4a017;
}
QCheckBox::indicator:checked, QRadioButton::indicator:checked {
  background: #d4a017;
  border-color: #d4a017;
}
QCheckBox:disabled, QRadioButton:disabled {
  color: #948a76;
}
QCheckBox::indicator:disabled, QRadioButton::indicator:disabled {
  background: #1a1815;
  border-color: #332e26;
}

/* ── Containers & chrome ─────────────────────────────────────────────── */

QGroupBox {
  border: 1px solid #332e26;
  border-radius: 10px;
  margin-top: 12px;
  padding: 12px;
}
QGroupBox::title {
  subcontrol-origin: margin;
  left: 12px;
  padding: 0 4px;
  color: #a89c86;
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

QStatusBar {
  background: #1a1815;
  border-top: 1px solid #332e26;
  color: #a89c86;
  font-size: 12px;
  min-height: 24px;
}
QStatusBar::item {
  border: none;
}

QToolBar {
  background: #1a1815;
  border-bottom: 1px solid #332e26;
  spacing: 4px;
  padding: 4px;
}
QToolButton {
  background: transparent;
  border: none;
  border-radius: 4px;
  padding: 6px;
  color: #a89c86;
}
QToolButton:hover {
  background: #221f1a;
  color: #e2d9c8;
}
QToolButton:pressed {
  background: #52504b;
}
QToolButton:checked {
  background: #2b271f;
  color: #d4a017;
}

QProgressBar {
  background: #2b271f;
  border: none;
  border-radius: 4px;
  height: 8px;
  text-align: center;
  color: transparent;
}
QProgressBar::chunk {
  background: #d4a017;
  border-radius: 4px;
}

QSlider::groove:horizontal {
  background: #2b271f;
  height: 4px;
  border-radius: 2px;
}
QSlider::sub-page:horizontal {
  background: #d4a017;
  border-radius: 2px;
}
QSlider::handle:horizontal {
  background: #d4a017;
  width: 14px;
  height: 14px;
  margin: -5px 0;
  border-radius: 7px;
}
QSlider::handle:horizontal:hover {
  background: #dbaf4f;
}

/* ── Item views ──────────────────────────────────────────────────────── */

QListWidget, QListView, QTreeView, QTableView {
  background: #1a1815;
  border: 1px solid #332e26;
  border-radius: 6px;
  alternate-background-color: #221f1a;
  outline: 0;
}
QListWidget::item, QListView::item, QTreeView::item {
  padding: 6px 8px;
  border-radius: 4px;
}
QListWidget::item:hover, QListView::item:hover, QTreeView::item:hover {
  background: #221f1a;
}
QListWidget::item:selected, QListView::item:selected, QTreeView::item:selected {
  background: #2b271f;
  color: #d4a017;
}
QTableView {
  gridline-color: #332e26;
  selection-background-color: #2b271f;
  selection-color: #f1e7c4;
}
QHeaderView::section {
  background: #221f1a;
  color: #a89c86;
  border: none;
  border-bottom: 1px solid #332e26;
  border-right: 1px solid #332e26;
  padding: 6px 8px;
  font-size: 12px;
  font-weight: 600;
}

/* ── Status text helpers (setProperty("class", ...) + re-polish) ─────── */

QLabel[class="success"] { color: #86b86b; }
QLabel[class="error"] { color: #e4635a; }
QLabel[class="warning"] { color: #e08a2b; }
QLabel[class="info"] { color: #6fa8c7; }
QLabel[class="muted"] { color: #a89c86; }
QLabel[class="code"] {
  font-family: "Iosevka", ui-monospace, "Courier New", monospace;
  background: #2b271f;
  border-radius: 4px;
  padding: 2px 6px;
}
