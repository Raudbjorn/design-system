// The Vernacular type moved to the vernacular runtime subsystem
// (@svnbjrn/design/vernacular). Re-exported here so the "theme's other half"
// discoverability — and any existing import path — keeps working.
export type { Vernacular } from '../vernacular/types.ts';
